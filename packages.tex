% Font related packages
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype} % To enable pdftex-specific typographic features

% etex extensions to use more registers.
\usepackage{etex}

% To enable links in PDF
%\usepackage[colorlinks=true]{hyperref}
\usepackage{hyperref}

% Create a new float type for code listings. Due to some bug, this needs to be
% put after the hyperref package is loaded.
\newfloat[chapter]{listing}{lst}{Listing}

% The varioref package allows references of the form ``on page ...'' etc.
\usepackage{varioref}

% Display label names as a small text in the margin.
%\usepackage[notref,notcite]{showkeys}

% To include PDFs.
\usepackage{graphicx}

% Permits per-chapter appendices
\usepackage{appendix}

% Allows more general enumerations
\usepackage{enumerate}

% For the formatting of URLs
\usepackage{url}

% Math-related packages
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amsthm}

% Set up the fancyvrb package for framed code listing boxes with line numbers.
\usepackage{fancyvrb}
\DefineVerbatimEnvironment{Code}{Verbatim}{gobble=2,numbers=left,frame=single,%
fontsize=\small,fontshape=n}
\newcommand\CodeInput[1]{\VerbatimInput[gobble=0,numbers=left,frame=single,fontsize=\small,fontshape=n]{#1}}

% Illustration-related packages
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{boxedminipage}
\usepackage[caption=false,font=footnotesize]{subfig}

% For the formatting of the code reference section.
\usepackage{manualmacros}
